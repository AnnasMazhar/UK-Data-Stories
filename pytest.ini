[pytest]
testpaths = tests
asyncio_mode = auto
markers = 
    integration: requires populated database

[coverage:run]
source = .
omit = 
    .venv/*
    tests/*
    */site-packages/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
